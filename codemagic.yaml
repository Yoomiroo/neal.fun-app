workflows:
  ios-release:
    name: iOS Release

    environment:
      xcode: latest

    scripts:
      - name: Install dependencies
        script: |
          npm install

      - name: Sync Capacitor iOS
        script: |
          npx cap sync ios

    artifacts:
      - build/ios/ipa/*.ipa